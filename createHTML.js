const members = require("./index");
const fs = require("fs");

function teamMember(teamMems) {
  for (let i = 0; i < teamMems.length; i++) {}
}

function createHtml() {
  fs.writeFile("teamRoster.html");
}
